# Small Service

This is an intentionally inconsistent documentation fixture for Ennodia.

The current default port is 3000. Configure another port with `PORT`.
The readiness response is `{ "status": "ok" }`.

The [migration record](./archive/migration.md) describes the previous release.
