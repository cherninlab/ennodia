# Try a review skill on one small function

Compare `retry.ts` with `contract.md`. The function has deliberate defects.
Run a plain review and a review with a native installed skill in separate tasks.
Use the same model, files, and request. Save both findings before opening `check.ts`.

The evaluator contains the expected cases. Keep it out of the review material.
This example demonstrates the workflow. It does not establish that a skill improves quality.
