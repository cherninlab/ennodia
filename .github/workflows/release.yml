name: Release

on:
  push:
    tags:
      - "v[0-9]*"
  workflow_dispatch:
    inputs:
      dist_tag:
        description: "npm dist-tag"
        type: choice
        required: true
        default: next
        options:
          - next
          - latest
      dry_run:
        description: "Run all gates but skip npm publish"
        type: boolean
        required: true
        default: true

permissions:
  contents: write
  id-token: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    name: Publish
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - name: Upgrade npm
        run: npm install -g npm@latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run release checks
        run: bun run release:check

      - name: Resolve release mode
        id: release_mode
        env:
          INPUT_DIST_TAG: ${{ github.event.inputs.dist_tag }}
          INPUT_DRY_RUN: ${{ github.event.inputs.dry_run }}
        run: |
          dist_tag="${INPUT_DIST_TAG:-next}"
          dry_run="${INPUT_DRY_RUN:-false}"

          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            if [[ "${GITHUB_REF_NAME}" == *-* ]]; then
              dist_tag="next"
            else
              dist_tag="latest"
            fi
            dry_run="false"
          fi

          echo "dist_tag=${dist_tag}" >> "$GITHUB_OUTPUT"
          echo "dry_run=${dry_run}" >> "$GITHUB_OUTPUT"

      - name: Dry-run publish
        if: steps.release_mode.outputs.dry_run == 'true'
        run: npm publish --dry-run --tag "${{ steps.release_mode.outputs.dist_tag }}"

      - name: Dry-run publish to JSR
        if: steps.release_mode.outputs.dry_run == 'true'
        run: npx jsr publish --dry-run

      - name: Publish to npm
        if: steps.release_mode.outputs.dry_run != 'true'
        run: npm publish --tag "${{ steps.release_mode.outputs.dist_tag }}"

      - name: Publish to JSR
        if: steps.release_mode.outputs.dry_run != 'true'
        run: npx jsr publish

      - name: Pack release artifact
        if: steps.release_mode.outputs.dry_run != 'true'
        run: npm pack

      - name: Create GitHub release
        if: steps.release_mode.outputs.dry_run != 'true' && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: "*.tgz"
          generate_release_notes: true
          prerelease: ${{ contains(github.ref_name, '-') }}
